package com.brouding.simpledialog.extra

enum class Type {
    GENERAL, LOADING, SELECTION, CUSTOM
}

enum class BtnAction {
    CONFIRM, CONFIRM_WITH_PERMANENT_CHECK, CONFIRM_WITHOUT_PERMANENT_CHECK, CANCEL, DIALOG_INSIDE, DIALOG_OUTSIDE
}

enum class DialogAction {
    SHOW, CANCEL, DISMISS
}